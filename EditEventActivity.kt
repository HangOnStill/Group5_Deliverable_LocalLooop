package com.example.localloop

import androidx.appcompat.app.AppCompatActivity

class EditEventActivity : AppCompatActivity() {
    // Use the same fields and logic as AddEventActivity.
    // OnCreate: If "eventId" in intent, load event and fill fields.
    // On save, update the event via EventRepository.updateEvent()
}
