<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="@android:color/white">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:titleTextColor="@android:color/white"
        android:elevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />
    <!-- Add other views here and also constrain them vertically -->



    <TextView
        android:id="@+id/nameEmailText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="17sp"
        android:textColor="#222222"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <TextView
        android:id="@+id/roleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/Role"
        android:textSize="14sp"
        android:textColor="#555"
        app:layout_constraintTop_toBottomOf="@id/nameEmailText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="6dp"/>

    <Button
        android:id="@+id/statusToggleBtn"
        android:layout_width="160dp"
        android:layout_height="400dp"
        android:text="@string/disable"
        app:layout_constraintTop_toBottomOf="@id/roleText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/deleteUserBtn"
        app:layout_constraintHorizontal_weight="1"
        android:layout_marginTop="14dp"
        android:layout_marginEnd="8dp"/>

    <Button
        android:id="@+id/deleteUserBtn"
        android:layout_width="160dp"
        android:layout_height="400dp"
        android:text="@string/delete"
        app:layout_constraintTop_toBottomOf="@id/roleText"
        app:layout_constraintStart_toEndOf="@id/statusToggleBtn"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_weight="1"
        android:layout_marginTop="14dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>